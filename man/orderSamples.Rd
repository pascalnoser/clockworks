% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CircadianData.R
\name{orderSamples}
\alias{orderSamples}
\alias{orderSamples,CircadianData-method}
\title{Order Samples in a CircadianData Object}
\usage{
orderSamples(x, by_columns, decreasing = FALSE, ...)

\S4method{orderSamples}{CircadianData}(x, by_columns, decreasing = FALSE)
}
\arguments{
\item{x}{A \code{CircadianData} object.}

\item{by_columns}{A character vector specifying the column name(s) in
\code{metadata(x)} to sort by. Sorting is done sequentially by these columns.}

\item{decreasing}{Logical vector indicating the direction of sorting for
each column specified in \code{by_columns}. If a single value, it is recycled.
Defaults to FALSE (ascending order) for all columns.}

\item{...}{Additional arguments (currently unused).}
}
\value{
A new \code{CircadianData} object with samples sorted according
to the specified criteria. The \code{dataset} columns and \code{metadata} rows
remain synchronized.
}
\description{
Reorders the samples (columns in \code{dataset}, rows in \code{metadata}) of a
\code{CircadianData} object based on the values in specified columns
of the metadata.
}
\examples{
# Create minimal reproducible data
set.seed(456)
counts <- matrix(rpois(80, lambda = 50), nrow = 10, ncol = 8,
                 dimnames = list(paste0("Feature", 1:10), paste0("Sample", 1:8)))
meta <- data.frame(
  row.names = paste0("Sample", 1:8),
  time = rep(c(12, 0, 6, 18), each = 2), # Unordered times
  subject_id = paste0("S", rep(1:4, 2)), # Unordered subjects
  group = rep(c("A", "B"), 4)
)
cd_obj <- CircadianData(counts, meta, experimentInfo = list(period = 24))

print("Original Metadata Order:")
print(metadata(cd_obj))

# Sort samples by time
cd_sorted_time <- orderSamples(cd_obj, "time")
print("Metadata sorted by time:")
print(metadata(cd_sorted_time))
print(colnames(dataset(cd_sorted_time))) # Check dataset colnames match metadata rownames

# Sort samples first by time (ascending), then by subject_id (descending)
cd_sorted_multi <- orderSamples(cd_obj, c("time", "subject_id"), decreasing = c(FALSE, TRUE))
print("Metadata sorted by time (asc) then subject_id (desc):")
print(metadata(cd_sorted_multi))

}
