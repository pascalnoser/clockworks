% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CircadianData.R
\name{plot_upset}
\alias{plot_upset}
\title{Create UpSet Plots Comparing Rhythm Detection Results}
\usage{
plot_upset(
  cd,
  pval_adj_cutoff = 0.05,
  n_features = NULL,
  compare = c("methods", "groups"),
  groups = NULL,
  methods = NULL,
  n_intersections = 10,
  include_all_sets = TRUE
)
}
\arguments{
\item{cd}{A \code{CircadianData} object containing results from rhythm
detection analyses.}

\item{pval_adj_cutoff}{A numeric value between 0 and 1 specifying the
adjusted p-value threshold for considering a feature as significant.
Defaults to 0.05.}

\item{n_features}{An alternative to \code{pval_adj_cutoff}. An integer specifying the
number of top features (ranked by adjusted p-value) to include in the plot for
each method/group. If provided, this will override the \code{pval_adj_cutoff}
filtering.}

\item{compare}{A character string specifying what to compare: \code{"methods"}
(default) compares different rhythm detection methods within each group
(if there are any), while \code{"groups"} compares different experimental groups
within each method.}

\item{groups}{An optional character vector of group names to include in the
plot(s). If \code{NULL} (default), all available groups are used.}

\item{methods}{An optional character vector of method names to include in the
plot(s). If \code{NULL} (default), all available methods are used.}

\item{n_intersections}{An integer specifying the maximum number of
intersections to display. Defaults to 10.}

\item{include_all_sets}{A logical value. If \code{TRUE} (default), ensures the
intersection containing all sets is included in the plot.}

\item{...}{Additional arguments passed to \code{\link[ComplexUpset]{upset}}.}
}
\value{
If a single method/group combination is plotted, returns a single UpSet plot
object (from \code{\link[ComplexUpset]{upset}}). If multiple combinations are
compared, returns a named list of UpSet plot objects.
}
\description{
Generates UpSet plots to visualize the overlap of significant features
(e.g., genes) across different rhythm detection methods or experimental groups..
}
\examples{
\dontrun{
  # Compare significant features across rhythm detection methods
  plot_upset(cd, pval_adj_cutoff = 0.05, compare = "methods")

  # Compare significant features across experimental groups
  plot_upset(cd, pval_adj_cutoff = 0.05, compare = "groups")

  # Focus on specific methods
  plot_upset(cd, methods = c("RAIN", "JTK_CYCLE"))
}

}
